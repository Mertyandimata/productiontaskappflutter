name: etsu
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  barcode_scanner: ^5.1.0
  flutter_barcode_scanner: ^2.0.0
  postgres: ^3.3.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Burada assets kısmı düzeltilmiş şekilde
  assets:
    - assets/logo2.png
